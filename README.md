# DB Syncer (Postgres)

Полноценный инструмент для безопасной синхронизации **схемы и данных** между тестовой и боевой PostgreSQL базой.

## 🔹 Основной функционал

- Сравнение **схемы БД** (таблицы, колонки, типы)
- Мягкая синхронизация: только добавление новых элементов, без удаления или изменения типа колонок
- Синхронизация данных
- Обработка конфликтов по `PRIMARY KEY`:
  - **skip** — оставить данные target
  - **overwrite** — заменить данными из source
  - **merge** — заполнить только пустые поля target
- Поддержка **составных PK** и **циклических FK**
- REST API + Web UI (FastAPI + HTMX)
- Логи всех изменений (`sync.log`)
- Возможность генерации тестовых данных для демонстрации
- Docker-ready (Linux/Windows)

---

## 🔹 Структура проекта

```
db-syncer/
├── api/
│   ├── main.py              # FastAPI backend
│   ├── routers/ 
│   │   ├── api_sync.py
│   │   └── web.py
│   ├── schemas/ 
│   │   └── sync.py
│   ├── templates/           # HTML-шаблоны
│   │   ├── index.html
│   │   ├── diff.html
│   │   ├── conflicts.html
│   │   └── ...
├── syncer/
│   ├── db_syncer.py         # Класс синхронизации
│   └── logging_config.py    # Конфиг логов
├── db-init/
│   ├── source/init.sql      # Скрипт генерации тестовой source DB
│   └── target/init.sql      # Скрипт генерации тестовой target DB
├── tests/
│   ├── conftest.py
│   ├── test_api_sync.py
│   └── ...                  # Автотесты
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── sync.log
└── README.md
```

---

## 🔹 Запуск с тестовыми БД

### 1. Активировать профиль `test_db`

```bash
docker-compose --profile test_db up --build
```

- Поднимутся две Postgres базы:
  - `source_db` (тестовая, эталон)
  - `target_db` (боевая)
  - `db-syncer` подключается автоматически

Чтобы очистить старые volumes:

```bash
docker-compose --profile test_db down -v
docker-compose --profile test_db up -d
```

### 1.1 Тестирование API /api/sync через curl

#### Локальный запрос:
```bash
curl -X POST http://127.0.0.1:8000/api/sync \
     -H "Content-Type: application/json" \
     -d '{
           "source_url": "postgresql://postgres:postgres@localhost:5433/source_db",
           "target_url": "postgresql://postgres:postgres@localhost:5434/target_db",
           "pk_strategy": "merge"
         }'
```
Пример ответа:
```
{
  "status": "ok",
  "schema_synced": true,
  "data_synced": true,
  "pk_strategy": "merge"
}
```

#### Через контейнер db-syncer:
```bash
curl -X POST http://db-syncer:8000/api/sync \
     -H "Content-Type: application/json" \
     -d '{
           "source_url": "postgresql://postgres:postgres@postgres_source:5432/source_db",
           "target_url": "postgresql://postgres:postgres@postgres_target:5432/target_db",
           "pk_strategy": "merge"
         }'
```

Пример ответа аналогичный.

### 1.2 Автотесты
```bash
pytest -v
```
- Тесты используют те же фикстуры `source_engine` и `target_engine`.

---

## 🔹 Web UI

```
http://localhost:8000
```
- Вводим URL БД для source и target.
- Доступ к документации FastAPI:
```
http://localhost:8000/docs
http://localhost:8000/redoc
```

### Основные шаги

1. **Get Diff** — сравнение схемы двух БД:
   - Добавление новых таблиц/колонок
   - Предложение удаления лишних элементов (только как предупреждение)
2. **Confirm Delete** — мягкая синхронизация схемы
3. **View Conflicts** — просмотр конфликтных записей по PK
4. Выбор стратегии: `skip`, `overwrite`, `merge`
5. **Apply Strategies** — синхронизация данных
6. **Run Full Sync** — полный прогон синхронизации схемы и данных

---

## 🔹 Тестирование стратегий PK

| PK | Source                    | Target                   | Комментарий |
|----|---------------------------|--------------------------|-------------|
| 1  | Alice                     | Alice PROD               | Конфликт    |
| 2  | Bob                       | Bob PROD (city=NULL)     | Конфликт, часть данных пустая |
| 3  | —                         | Charlie                  | Только target |
| 4  | David                     | —                        | Только source |
| 5  | Eve                       | —                        | Только source |

- **skip** → конфликты остаются как в target
- **overwrite** → конфликты заменяются данными из source
- **merge** → только пустые поля target заполняются из source

---

## 🔹 Запуск без тестовых БД

```bash
docker-compose up --build
```
- Поднимается только `db-syncer`
- Подключение к существующим БД через Web UI

---

## 🔹 Локальный запуск без Docker

```bash
pip install -r requirements.txt
uvicorn api.main:app --reload
```
- Подключение к любым PostgreSQL БД через Web UI

---

## 🔹 Логирование

Все изменения логируются в `sync.log`:
```text
2026-02-06 18:00:12 INFO Syncer: Added column 'age' to table 'users'
2026-02-06 18:01:05 INFO Syncer: Overwrote record PK=1 in table 'users'
```

---

## 🔹 Контакты / поддержка

- Скрипт полностью автономен, все зависимости в `requirements.txt`
- Docker гарантирует одинаковое поведение на Linux и Windows (WSL2)
- Web UI и API позволяют безопасно просматривать и управлять процессом синхронизации